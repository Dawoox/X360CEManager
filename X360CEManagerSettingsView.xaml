<UserControl x:Class="X360CEManager.X360CEManagerSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel>
        <TextBlock Text="Path to the x360ce executable:"/>
        <TextBox Text="{Binding Settings.x360cePath}"/>
        <TextBlock Text="Start X360CE with Playnite:"/>
        <CheckBox IsChecked="{Binding Settings.startWithPlaynite}"/>
        <TextBlock Text="Start X360CE with selected games:"/>
        <CheckBox IsChecked="{Binding Settings.startWithSelectedGames}"/>
        <TextBlock Text="Start X360CE with all games:"/>
        <CheckBox IsChecked="{Binding Settings.startWithAllGames}"/>
    </StackPanel>
</UserControl>